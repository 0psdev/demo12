apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "demo12.fullname" . }}-config
  labels:
    {{- include "demo12.labels" . | nindent 4 }}
data:
  envVarName: {{ .Values.envVarName | quote }}
  state: {{ .Values.state | quote }}
  color: {{ .Values.color | quote }}